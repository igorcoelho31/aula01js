// Importa a classe
import { PessoaDois } from "./pessoa2.js";

// Instanciar a classe
const aluno = new PessoaDois("Gustavo", 22);
  
console.log ("Nome:" + aluno.falarNome() + '     ' + "Idade:" + aluno.falarIdade());
